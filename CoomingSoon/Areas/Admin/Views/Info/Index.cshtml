@model HomeVM
@{
    ViewData["Title"] = "Index";
}

<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Info</h4>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>LeftHead</th>
                                <th>LeftContent</th>
                                <th>RightContent</th>
                                <th>RightDownContent</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>@Model.Info.LeftHead</td>
                                <td>@Model.Info.LeftContent</td>
                                <td>@Model.Info.RightContent</td>
                                <td>@Model.Info.RightDownContent</td>
                            </tr>
                        </tbody>
                        <a asp-action="UpdateInfo" asp-route-id="@Model.Info.Id">Update</a>
                    </table>
                </div>
            </div>
        </div>
    </div>
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Option</h4>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Options</th>
                                <th>Setting</th>
                            </tr>
                        </thead>
                        <tbody>

                            @foreach (var option in Model.RightOptions)
                            {
                            <tr>
                                <td>@option.Option</td>
                                <td>
                                    <a asp-action="UpdateOption" asp-route-id="@option.Id">Update</a>   
                                    
                                </td>
                            </tr>
                                    }
                               
                            </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

